<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.com.famss10">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConnexionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".CountsActivity" />
        <activity android:name=".ActivityDatabase" />
        <activity android:name=".CountActivity" />
        <activity android:name=".NewCountActivity" />
        <activity android:name=".TransactionActivity" />
        <activity
            android:name=".menu"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SummaryActivity" />
        <activity android:name=".SummarySwipeLeftActivity" />
        <activity android:name=".SummaryCategoryTransactionActivity" />
        <activity android:name=".SummaryPieChartActivity" />
        <activity android:name=".Superviseur" />
        <activity android:name=".SuppressionCompte"></activity>
    </application>

</manifest>